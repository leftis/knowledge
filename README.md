# Leftis Knowledge

## How do I install these formulae?

`brew install leftis/knowledge/<formula>`

Or `brew tap leftis/knowledge` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
